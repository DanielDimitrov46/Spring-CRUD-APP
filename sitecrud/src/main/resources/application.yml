spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password:
        url: jdbc:mysql://localhost:3306/sitecrud
        username: root
    jpa:
        hibernate:
            ddl-auto: create-drop
    server:
        error:
            include-message: always
            port: 8080
